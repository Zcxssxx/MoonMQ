# Security policy

MoonMQ is initially an in-memory protocol and broker library intended for local demos and embedded use. The first release does not promise production-grade authentication, authorization, TLS, persistence, or denial-of-service hardening.

Please report parser crashes, unbounded allocation paths, frame-size bypasses, or message-isolation issues through a private GitHub security report when available, or open a public Issue for non-sensitive problems.
